extends layout

block content
  .main
    h1= title
    if brands_list.length 
        ul 
            each brand in brands_list 
                li 
                    a(href=brand.url) #{brand.name}
    else    
        p There s no Brands
    a.button.is-primary(href='/brands/create') Add New Brand