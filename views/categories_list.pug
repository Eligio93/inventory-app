extends layout

block content
  .main
    h1= title
    if categories_list.length 
        ul 
            each category in categories_list 
                li 
                    a(href=category.url) #{category.name}
        a.button.is-primary(href='/categories/create') Add New Category
    else    
        p There s no Categories